# ClawCore Paint

간단한 웹 기반 그림판 MVP.

## 기능
- 브러시
- 지우개
- 색상 선택
- 브러시 두께 조절
- 배경색 선택
- 전체 지우기
- PNG 저장

## 실행
ClawCore 루트에서:

```bash
python3 -m http.server 8000
```

그 다음 브라우저에서:
- <http://127.0.0.1:8000/apps/paint/>

또는 임의의 정적 서버로 `apps/paint/index.html`을 열어도 됨.
